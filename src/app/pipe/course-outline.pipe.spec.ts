import { CourseOutlinePipe } from './course-outline.pipe';

describe('CourseOutlinePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseOutlinePipe();
    expect(pipe).toBeTruthy();
  });
});
